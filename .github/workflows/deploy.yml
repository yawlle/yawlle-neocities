name: Deploy to Neocities

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Neocities
        uses: jonchun/deploy-to-neocities@v1
        with:
          api_key: ${{ secrets.NEOCITIES_API_TOKEN }}
